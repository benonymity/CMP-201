#include <iostream>
#include <list>

// This has been suceeded
// template <class T>
template <typename T>
class Node {
    public:
    private:
        int value;
        Node* next;
};

int main() {
    std::list<int>{};
    exit(1);
}